# TEST_SPEC.md — tegaki-yomi

<!-- scaffold template v1.8.0 から展開(2026-08-17) -->

## 実行規約

- `npx vitest run` を stage 3–5 の判定に使用。カバレッジ判定は `npx vitest run --coverage`
- テストは `src/core/__tests__/*.test.ts`(+ `src/lib/*.test.ts`)に置き、describe に対応 T-ID をコメントで残す
- **二実装照合の規約**: フィクスチャのロジット・精度は Python 側が「丸め済み重みを読み戻して」
  計算した値であること(丸め前の重みで計算すると照合に失敗する — 生成スクリプト側の契約)
- 前処理の期待値はテスト内で手計算し、導出をコメントに残す
- 縁(空入力・1 点入力)は正常系として挙動を固定する(N-05)

## ケース一覧

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-001 | F-03/G-05 | 重み JSON の形状 | W1: 128×784・b1: 128・W2: 10×128・b2: 10・全て有限値 |
| T-010 | F-03 | softmax | 総和 1・単調・大入力でも有限(最大値シフト) |
| T-011 | F-03/G-05 | forward の決定論 | 同一入力で深い等値・logits は 10 要素 |
| T-020 | F-04/G-04 | 質量中心 | 合成 2 画素画像で手計算一致 |
| T-021 | F-04/G-04 | 外接矩形と縦横比保存縮小 | 縦長ストロークが 20px 高に縮み、幅は比例(面積平均の値も手計算) |
| T-022 | F-04/N-05 | 空入力・1 点入力 | 空は未検出(null)・1 点は 28×28 内に有効配置 |
| T-023 | F-04 | 前処理の値域 | 出力 784 要素・[0,1]・インク総量 > 0 |
| T-100 | G-01 | フィクスチャ 20 件のロジット照合 | TS forward が Python 再計算値と相対誤差 < 1e-9 |
| T-101 | G-02 | 同梱テスト 500 件 | TS の正解数 = Python 記録値・精度 ≥ 96% |
| T-102 | G-03 | assets メタ | 全 10,000 件テスト精度 ≥ 97% が記録されている |
| T-080 | F-09 | FOOTER_LINKS | 5 件・全 https・ラベル非空・読ませ方≠設計図 |

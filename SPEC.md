# SPEC.md — tegaki-yomi

<!-- scaffold template v1.8.0 から展開(2026-08-17)。以後このファイルはプロジェクトが育てる -->

## 1. 目的

Canvas に描いた手書き数字を、MNIST で学習済みの小型ニューラルネット(MLP 784→128→10)が
その場で読む認識アプリ。学習は Python(numpy)でビルド時に行い、ブラウザには重みだけを載せる。
正しさの正本は**二実装照合オラクル** — TypeScript 推論が Python 学習側の出力(ロジット・精度)と
一致することをテストで保証する。推論・前処理の中身(モデルが見る 28×28、隠れ層の反応、
重みテンプレート)をすべて画面に見せる。ブラウザ完結・静的エクスポート。

## 2. 機能要求

| ID | 要求 | 優先度 |
|---|---|---|
| F-01 | 学習パイプライン(`training/train_mnist.py`・numpy のみ): MNIST を学習し、丸め済み重み JSON・照合フィクスチャ(丸め済み重みで再計算したロジット 20 件)・テスト部分集合(500 件)を出力する。シード固定で再現可能 | must |
| F-02 | モデル: MLP 784 → 128(ReLU)→ 10(softmax)。テスト精度 ≥ 97%(全 10,000 件・Python 側で計測し assets に記録) | must |
| F-03 | TS 推論(純関数): 丸め済み重み JSON を読み、forward(logits / softmax / argmax)を実装 | must |
| F-04 | MNIST 互換前処理(純関数): 280×280 グレースケール → インクの外接矩形 → 長辺 20px へ縦横比保存で縮小(面積平均)→ 28×28 に質量中心で配置。空入力は「未検出」を返す | must |
| F-05 | 手書きキャンバス: ポインタ描画(太筆)・クリア。描き終わるたびに自動で認識 | must |
| F-06 | 結果表示: 0〜9 の確率バー(argmax 強調)+ モデルが見た 28×28 のプレビュー | must |
| F-07 | 中身の可視化: 活性の大きい隠れユニット上位 8 個の重みテンプレート(28×28 ヒートマップ)と活性値 | must |
| F-08 | お手本モード: 同梱テスト画像(500 件)から 1 枚をキャンバスへ読み込み、正解ラベルと認識結果を並べる | must |
| F-09 | フッタ: MIT License・GitHub・操作説明(読ませ方)・設計図・App Menu の 5 リンク | must |

## 3. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | 静的エクスポート(`output: "export"`)。推論は全てブラウザ内・外部通信なし | `next build` が out/ を生成 |
| N-02 | `src/core` は純関数のみ。カバレッジ lines/functions/statements ≥ 90%, branches ≥ 85%(重み等のデータ資産は対象外) | eslint 境界規則 + vitest coverage 閾値 |
| N-03 | 認識は描画イベント終端で実行(MLP forward は 1ms 級で毎回再計算してよい)。Canvas API は effect / ハンドラ内のみ | HC-002 予防適用 |
| N-04 | ポインタ・タッチ両対応(指でも描ける) | 手動確認 |
| N-05 | 縁(空キャンバス・1 点だけの入力)は正常系として仕様化 | core テスト |

## 4. 品質基準

`npm run verify` green を全ループの完了条件とする(内訳は AGENTS.md §3)。

二実装照合ゲート(Python が正、TS が写し — 同じ丸め済み重みを使う):

| ID | ゲート | 基準 |
|---|---|---|
| G-01 | ロジット照合 | フィクスチャ 20 件で TS forward のロジットが Python 再計算値と相対誤差 < 1e-9 で一致 |
| G-02 | 精度照合 | 同梱テスト 500 件で TS の argmax 正解数が Python 記録値と完全一致、かつ精度 ≥ 96% |
| G-03 | 全体精度 | Python 側の全 10,000 件テスト精度 ≥ 97%(assets のメタに記録された値を検査) |
| G-04 | 前処理の手計算一致 | 質量中心・外接矩形・縦横比保存縮小を極小の合成画像で手計算照合。空入力は未検出 |
| G-05 | 決定論 | 同一入力の推論・前処理が深い等値。重み JSON の形状(784×128, 128, 128×10, 10)を検査 |

## 5. スコープ外

- ブラウザ内での学習・再学習(学習はビルド時の Python のみ)
- CNN・データ拡張・アンサンブル(MLP 1 本。精度向上は将来ループ候補)
- 複数文字・文字列認識・ひらがな等の他文字種
- 描画履歴の永続化・効果音
